# dialogs example

This example is a separate workspace member so it uses `shiguri-01/desktop`
like an external application would. It demonstrates showing native message
dialogs from a host IPC handler and returning the selected button to a MoonBit
JS frontend.

```powershell
just run-dialogs-example
```
