# Markdown Notes

Practical desktop example using MoonBit native host code, Luna UI, Markdown
rendering, typed IPC, native file dialogs, and local state persistence.

Build the frontend first:

```sh
npm --prefix examples/markdown-notes install
just build-markdown-notes-frontend
```

Run the desktop app:

```sh
just run-markdown-notes-example
```
