name = "shiguri-01/app-packager"

version = "0.1.0"

readme = "README.mbt.md"

repository = ""

license = "Apache-2.0"

keywords = [ "desktop", "packaging" ]

description = "MoonBit desktop application packager"

preferred_target = "native"

supported_targets = "+native"

options(
  source: "src",
)
